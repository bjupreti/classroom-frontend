import Container from './container/Container';

/**
 * - We will be placing common components in this section.
 * - Common components mean a component that is used on at least two pages.
 */
export { Container };
